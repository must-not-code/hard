.col-md-8
  .well
    %h2
      = link_to 'Команда Hard Random', teams_path
    %hr
    .overflow
      - @teams.each do |team|
        .col-md-6
          = link_to image_tag(team.logo, class: 'game-logo'), team_path(team.game)
