.col-md-8
  .well
    = render 'navbar'
    %table.table.tourney-table
      - @tournaments.each do |tournament|
        %tr
          %th
            = link_to tournament.title, tournament_path(tournament.id)
          %th
            .pull-right
              = tournament.beginning.strftime("%d.%m.%Y в %H:%M")
